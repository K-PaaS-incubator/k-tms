#
# 이 파일은 제품의 디버깅 옵션을 설정하는 파일입니다.
#

comment "Common library"

config CONFIG_DEBUG_LIBRARY_TSNLS
	bool "    Library TSNLS Debugging"
	default n
	help
		라이브러리 TSNLS에 대한 디버깅 로그 출력 여부를 선택합니다. 기본은 아니오 입니다.

config CONFIG_DEBUG_CC_AUDIT_FAIL
	bool "    Debugging for CC AUDIT FAIL"
	default n
	help
		CC 인증용 이미지로 Audit 실패를 강제로 할 때 선택합니다. 기본은 아니오 입니다.

